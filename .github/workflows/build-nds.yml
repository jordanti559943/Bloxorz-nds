name: Build NDS

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Install BlocksDS SDK
        uses: blocksds/sdk@main

      - name: Build
        run: make
